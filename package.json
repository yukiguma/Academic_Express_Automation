{
  "name": "academic-express-automation",
  "version": "1.1.1",
  "private": true,
  "scripts": {
    "version:check": "node scripts/check-release-version.js",
    "version:sync": "node scripts/set-release-version.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
